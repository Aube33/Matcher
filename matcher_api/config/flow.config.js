module.exports = {
    requestStackPerFlow: 3, // Number of pre-recommended profiles per stack of flow
    profileConsultedDelay: 12 * 60 * 60 * 1000 //12h // Delay before reset of consulted profile in flow
}